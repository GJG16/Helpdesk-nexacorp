# Contribuir al repositorio

1. Usar ramas `feature/` desde `develop`.
2. Abrir PR hacia `develop` y marcar como draft si está en progreso.
3. Ejecutar tests localmente antes de push: `pytest backend/tests.py` y `npm run build`.
4. Mantener PRs pequeños y descriptivos.
