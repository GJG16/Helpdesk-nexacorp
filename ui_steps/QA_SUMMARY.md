# QA Summary - Cambios aplicados

## Resumen de cambios

- Backend: se impide que usuarios con rol `admin` creen tickets. Archivo modificado: `backend/routes/tickets.py`.
- Frontend:
  - Interceptor de auth mejorado: intenta `refreshToken()` ante 401 y reintenta la petición; solo hace logout si falla el refresh. Archivo: `frontend/src/app/interceptors/auth.interceptor.ts`.
  - Componentes `Dashboard` y `TicketsList` esperan a `currentUser$` antes de cargar datos, evitando peticiones sin token y estados `Cargando...`. Archivos: `frontend/src/app/components/dashboard/dashboard.component.ts`, `frontend/src/app/components/tickets/tickets-list/tickets-list.component.ts`.
  - Modal de creación de ticket ya muestra validaciones y desactiva submit cuando hay errores o mientras carga. Archivo: `frontend/src/app/components/tickets/ticket-create-modal/*`.

## Pasos ejecutados

1. Ejecutado `python backend/e2e_flow.py` para validar flujo API (seed, crear/asignar/comentar/resolver/eliminar). Resultado: OK.
2. Ejecutado `python ui_steps/capture_ui_screenshots.py` para capturar pantallas por rol. Capturas guardadas en `ui_steps/screenshots/`.

## Capturas

- ui_steps/screenshots/user_tickets.png
- ui_steps/screenshots/user_tickets_kanban.png
- ui_steps/screenshots/user_dashboard.png
- ui_steps/screenshots/agent_tickets.png
- ui_steps/screenshots/agent_tickets_kanban.png
- ui_steps/screenshots/agent_dashboard.png
- ui_steps/screenshots/admin_tickets.png
- ui_steps/screenshots/admin_tickets_kanban.png
- ui_steps/screenshots/admin_dashboard.png

## Recomendaciones siguientes

- Aplicar control en backend para registros de auditoría al crear tickets (si aún no existe) para monitorizar quién crea qué.
- Revisar flujo de refresh token en situaciones de concurrencia (evitar múltiples refresh simultáneos).
- Realizar pequeños ajustes UX: mensajes de éxito mejor visibles, confirmar creación con enlace al ticket.



Generated by automated QA run.
